# Access operation - O(1)
def access(arr, index):
    return arr[index]

# Example usage
arr = [1, 2, 3, 4, 5]
print("Accessing element at index 2:", access(arr, 2))  # Should print 3

# Time Complexity: O(1) - Accessing an element by index takes constant time.
